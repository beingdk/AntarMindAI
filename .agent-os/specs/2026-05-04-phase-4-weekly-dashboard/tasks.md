<!-- Generated by AI on 05/04/2026 -->

# Spec Tasks

## Tasks

- [ ] 1. Implement weekly reflection domain models and aggregation service
  - [ ] 1.1 Write unit tests for `WeeklyReflectionService`: correct week boundary calculation, category distribution percentages, daily sentiment grouping, peak hour computation, zero-thought week returns empty aggregations
  - [ ] 1.2 Create `WeeklyReflection`, `CategoryCount`, `DailySentiment`, `HourlyActivity` records in `src/AntarMindAI.Api/Services/Reflections/`
  - [ ] 1.3 Create `IWeeklyReflectionService` interface
  - [ ] 1.4 Implement `WeeklyReflectionService` with week boundary logic (Monday–Sunday UTC) and all aggregations
  - [ ] 1.5 Register `WeeklyReflectionService` as scoped in `Program.cs`
  - [ ] 1.6 Verify all reflection service unit tests pass

- [ ] 2. Implement optional AI summary service
  - [ ] 2.1 Write unit tests for `NullAiSummaryService` (always returns null) and verify `OpenAiSummaryService` gracefully returns null on exception
  - [ ] 2.2 Create `IAiSummaryService` interface
  - [ ] 2.3 Implement `NullAiSummaryService` (default — always returns null)
  - [ ] 2.4 Implement `OpenAiSummaryService` constructing a prompt from aggregated stats only (no raw thought text); catch all exceptions and return null
  - [ ] 2.5 Add `AI:WeeklySummaryEnabled` and `AI:ApiKey` to `appsettings.json` (defaults: false, empty)
  - [ ] 2.6 Register correct implementation in `Program.cs` based on `AI:WeeklySummaryEnabled` and `AI:ApiKey` configuration
  - [ ] 2.7 Verify AI service unit tests pass

- [ ] 3. Implement ReflectionsController
  - [ ] 3.1 Write integration tests for `GET /api/reflections/weekly` (no date param, valid date param, invalid date param format, week with no thoughts)
  - [ ] 3.2 Create `ReflectionModels.cs` with all response records
  - [ ] 3.3 Implement `ReflectionsController` with `GetWeeklyReflectionAsync` action; parse optional `weekDate` query param; return 400 on invalid format
  - [ ] 3.4 Verify all controller integration tests pass

- [ ] 4. Install chart library and build Dashboard page
  - [ ] 4.1 Run `npm install @mui/x-charts` in `frontend/`
  - [ ] 4.2 Create `frontend/src/api/reflections.ts` with `getWeeklyReflection` API client function
  - [ ] 4.3 Create `CategoryChart` component using MUI X `PieChart` showing category distribution
  - [ ] 4.4 Create `SentimentTrendChart` component using MUI X `LineChart` with 3 series (Positive, Negative, Neutral) by day of week
  - [ ] 4.5 Create `ActivityHeatmap` component using MUI X `BarChart` showing thought count by hour
  - [ ] 4.6 Create `PatternHighlights` component showing top 3 insights as MUI `Card` items
  - [ ] 4.7 Create `DashboardPage` assembling all four components with `useQuery` for reflection data; show `CircularProgress` while loading
  - [ ] 4.8 Add AI summary section: show text if `aiSummary` is non-null; show "AI Summary Not Configured" chip if null
  - [ ] 4.9 Add `/dashboard` route to `App.tsx`
  - [ ] 4.10 Add Dashboard navigation link to `AppHeader`

- [ ] 5. Verify full integration and run all tests
  - [ ] 5.1 Run `npm run build` — verify no TypeScript errors after adding `@mui/x-charts`
  - [ ] 5.2 Run `dotnet test` — confirm all tests pass with 0 failures
  - [ ] 5.3 Run `npm run lint` — confirm no ESLint errors
  - [ ] 5.4 Manually verify: Dashboard page loads, all four charts render with data, empty state shown when no thoughts exist for the week
