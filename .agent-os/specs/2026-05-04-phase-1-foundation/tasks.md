<!-- Generated by AI on 05/04/2026 -->

# Spec Tasks

## Tasks

- [x] 1. Define domain model and repository interface
  - [x] 1.1 Write unit tests for `ThoughtEntry` validation constraints (text length, required fields)
  - [x] 1.2 Create `ThoughtEntry` model in `src/AntarMindAI.Api/Models/ThoughtEntry.cs`
  - [x] 1.3 Create `IThoughtRepository` interface with `CreateAsync`, `GetByUserAsync`, `DeleteAsync` in `src/AntarMindAI.Api/Repositories/`
  - [x] 1.4 Verify model tests pass

- [x] 2. Implement InMemoryThoughtRepository
  - [x] 2.1 Write unit tests for `InMemoryThoughtRepository` covering create, list (pagination), and delete
  - [x] 2.2 Implement `InMemoryThoughtRepository` using `ConcurrentDictionary` keyed by `UserId`
  - [x] 2.3 Implement pagination (skip/take) and reverse-chronological sort in the in-memory list
  - [x] 2.4 Verify all repository unit tests pass

- [x] 3. Implement AzureTableStorageThoughtRepository
  - [x] 3.1 Write integration tests for `AzureTableStorageThoughtRepository` using `Azurite` or real connection string (skipped in CI if env var absent)
  - [x] 3.2 Add `Azure.Data.Tables` NuGet package to `AntarMindAI.Api.csproj`
  - [x] 3.3 Implement `AzureTableStorageThoughtRepository` with inverted-tick `RowKey` for reverse-chronological order
  - [x] 3.4 Implement continuation token-based pagination for Table Storage queries
  - [x] 3.5 Register both repositories in `Program.cs` — select based on `AZURE_TABLE_STORAGE_CONNECTION_STRING` env var
  - [x] 3.6 Verify repository selects `InMemoryThoughtRepository` when connection string is absent

- [x] 4. Implement ThoughtsController and request/response models
  - [x] 4.1 Write integration tests for all three endpoints using `TestApiFactory` (WebApplicationFactory)
  - [x] 4.2 Create `ThoughtModels.cs` with `CreateThoughtRequest`, `ThoughtResponse`, `PagedThoughtsResponse` records
  - [x] 4.3 Implement `ThoughtsController` with `CreateThoughtAsync`, `GetThoughtsAsync`, `DeleteThoughtAsync`
  - [x] 4.4 Validate `text` length (1–2000 chars) and return standard 400 error response
  - [x] 4.5 Return 404 standard error response when deleting a non-existent or unowned thought
  - [x] 4.6 Add `Cache-Control: no-store` header to all responses
  - [x] 4.7 Verify all controller integration tests pass

- [x] 5. Build frontend thought capture and list UI
  - [x] 5.1 Create `frontend/src/api/thoughts.ts` with typed API client functions using `fetch` (no direct calls in components)
  - [x] 5.2 Create `ThoughtInput` component with MUI `TextField` + `Button`; uses `useMutation` to POST; clears on success; shows error `Alert` on failure
  - [x] 5.3 Create `ThoughtCard` component displaying text, relative timestamp, and delete `IconButton`
  - [x] 5.4 Create `ThoughtList` component using `useQuery` to fetch paginated thoughts; renders `ThoughtCard` items; shows loading state
  - [x] 5.5 Create `ThoughtsPage` assembling `ThoughtInput` + `ThoughtList`; add route `/thoughts` in `App.tsx`
  - [x] 5.6 Add navigation link to `/thoughts` from `AppHeader`
  - [x] 5.7 Run `npm run build` and verify no TypeScript errors
  - [x] 5.8 Verify all frontend functionality works end-to-end: submit, list, delete, pagination

- [x] 6. Verify full integration and run all tests
  - [x] 6.1 Run `dotnet test` — confirm all backend tests pass with 0 failures
  - [x] 6.2 Run `npm run lint` — confirm no ESLint errors
  - [x] 6.3 Run the app locally and manually verify: create thought, see in list, delete thought, app works without Azure connection string
